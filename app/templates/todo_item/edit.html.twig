{% include 'default/_form.html.twig' with {
    page_title: 'title.todo_edit'|trans({'%id%': todo.id|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'todo_index',
} %}