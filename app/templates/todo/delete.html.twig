{% include 'default/_form.html.twig' with {
    page_title: 'title.todo_item_delete'|trans({'%id%': todo_item.id|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'todo_index',
} %}
